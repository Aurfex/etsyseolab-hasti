/*
📂 Module: review-ai-respond.ts (AI Review Assistant)
Purpose:
این ماژول یک سیستم کامل برای مدیریت چرخه حیات نظرات مشتریان است. این سیستم با معماری سرویس‌گرا و آماده پروداکشن طراحی شده تا به راحتی به دیتابیس واقعی، سرویس‌های خارجی و سیستم احراز هویت متصل شود.

What it does:
۱. **معماری مبتنی بر سرویس (Service-Oriented):**
    - **سرویس دیتابیس (`databaseService.ts`):** منطق داده از API جدا شده و آماده اتصال به MongoDB یا PostgreSQL است.
    - **سرویس Etsy (`etsyService.ts`):** منطق ارسال پاسخ به Etsy در یک سرویس مجزا قرار گرفته تا اتصال به API واقعی آسان شود.
    - **سرویس نوتیفیکیشن (`notificationService.ts`):** ارسال هشدارها (مثلاً به Slack) توسط یک سرویس اختصاصی مدیریت می‌شود.

۲. **احراز هویت (JWT):**
    - تمام Endpoints های API اکنون به صورت شبیه‌سازی شده نیازمند توکن JWT در هدر Authorization هستند و برای اتصال به سیستم احراز هویت واقعی آماده‌اند.

۳. **تحلیل و تولید پاسخ با AI:**
    - نظرات جدید را از طریق API دریافت می‌کند.
    - با استفاده از مدل AI، احساسات و زبان نظر را تشخیص می‌دهد و پاسخ متناسب تولید می‌کند.
    - نظرات منفی را به صورت خودکار "نشانه‌گذاری" (Flag) کرده و از طریق سرویس نوتیفیکیشن هشدار ارسال می‌کند.

۴. **داشبورد مدیریت با تأیید انسانی (Human-in-the-Loop):**
    - مدیر می‌تواند پاسخ AI را ویرایش، تأیید یا رد کند.
    - **هیچ پاسخی بدون تأیید دستی به اتسی ارسال نمی‌شود.**

۵. **تاریخچه و لاگ حسابرسی (Audit Log):**
    - تمام اقدامات (تولید، ویرایش، تأیید، ارسال به اتسی، ارسال نوتیفیکیشن) با جزئیات کامل لاگ می‌شود.

API Endpoints:
- `GET /api/reviews` - دریافت لیست کامل نظرات و وضعیت آنها (نیازمند توکن)
- `POST /api/reviews` - انجام تمام عملیات نوشتن مانند تولید پاسخ، آپدیت وضعیت و ویرایش متن (نیازمند توکن)
*/